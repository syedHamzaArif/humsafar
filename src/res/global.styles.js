import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
    image: {
        width: '100%',
        height: '100%',
        flex: null,
    },
    row: {
        flexDirection: 'row',
        alignItems: 'center',
    },
});

export default globalStyles;
